# aeox-chat 本地说明文档

> 多 Provider / 多 Key 聚合的 Web AI 聊天站点 · chat.aeox.uk · AEOX 家族成员
>
> 本文档是项目的操作手册：本地运行、后台使用、部署上线、常见问题排查都在这里。

---

## 0. 当前状态（2026-09-20）

- **v0.1-beta 已上线**：https://chat.aeox.uk （Hostinger Web App，Hono 预设，运行时 Node v24.6.0）
- **2026-09-20 v0.1.1**：esbuild 由 ^0.25.0 升级至 ^0.28.1（实际 0.28.2），修复 Hostinger 漏洞扫描报告的 GHSA-gv7w-rqvm-qjhr（该 advisory 针对 Deno 分发路径且已被官方撤回，本项目仅用 Node 构建路径，无实际风险，升级仅为过检）；已重新部署，线上 Key 与配置保留
- 生产后台已录入 **2 个 API Key**，连接测试与线上对话冒烟均通过
- SEO 四件套（`robots.txt` / `sitemap.xml` / `llms.txt` / `ai.txt`）已随部署生效，后台可编辑
- 本地与生产 `MASTER_KEY` 不同，两边数据库的 Key 密文互不通用；生产密钥明文存于本地 `生产环境变量.txt`（gitignore，勿上传）

---

## 1. 项目定位

aeox-chat 是一个**技术展示型** AI 聊天站点，核心卖点是"多引擎"：把多家 OpenAI 兼容的大模型服务商（DeepSeek、Qwen、NVIDIA NIM、Groq、OpenRouter、agnes-ai、unorouter、AMD 等）的免费额度聚合到一个聊天界面背后，由服务端 Key 池自动轮换、故障转移、预算管控。

- 域名：chat.aeox.uk
- 形态：无注册的匿名聊天（Cookie 会话 + 每日限额），纯文字对话，单一普通模式
- 对话内容只存浏览器 localStorage，服务端不留存
- **当前为 v0.1-beta 测试版**（状态栏徽章标注）：全站明示"技术展示、数据不保存"；数据层完全可抛弃 — 随时可删 `data/` 重建，只需在后台重录 API Key
- 服务器数据自动清理：事件日志保留最近 500 条；用量统计保留 90 天；配额计数保留 7 天（启动时 + 每日定时执行）；**清理周期在用户端可见** — hub 首页隐私说明行、聊天页输入区下方提示、引擎页脱敏说明均标注
- `demo/` 目录是四个概念演示页（纯静态、模拟数据），正式站点继续挂在 `/demo` 路径公开
- 超出聊天范围的能力（文件上传、历史同步、分享、语音等）UI 上以禁用态 + "coming soon" 标注，不实现

## 2. 功能清单

### 公开层（无需登录）

| 功能 | 路径 | 说明 |
|---|---|---|
| Hub 首页 | `/` | 落地页：hero + 引擎实时 widget（2 秒轮询真实数据）、两张卡片入口（chat / engine）、引擎原理三步说明 |
| 聊天页 | `/chat` | 三区布局（侧栏 / 对话流 / 输入区），SSE 流式输出，引擎徽标行显示本次命中的 provider/model/key 序号/耗时/token，支持展开 trace；中英双语 |
| 引擎控制台 | `/engine` | 公开**脱敏**视图：Key 机架（只显示 provider + 序号如 `deepseek #01` + 状态）、路由事件流、聚合指标；2 秒轮询刷新 |
| 概念演示 | `/demo/` | 四个静态演示页（index / chat / engine / admin），模拟数据。服务端自动注入悬浮标识「CONCEPT DEMO · 概念演示」+「返回线上」按钮，用户不会与真实站点混淆；已被 robots.txt 屏蔽不参与 SEO；仅白名单 4 页可访问，其余 /demo/* 返回 404 |

### 后台层（密码登录，入口隐藏）

| 功能 | 路径 | 说明 |
|---|---|---|
| 登录 | `/admin` | 导航栏不展示入口。进入方式：① 手动输入 URL；② 在任意公开页**连点状态栏的 `v0.1-beta` 版本徽章 3 次**（1.2 秒内）自动跳转。密码为环境变量 `ADMIN_PASSWORD`，登录接口限速 5 次/10 分钟/IP |
| Key 管理 | 后台内 | 每个 Key 绑定：Provider 名称、Base URL、默认模型、优先级、月预算；支持启停、删除、连接测试 |
| 引擎日志 | 后台内 | 路由 / 故障转移 / 冷却 / 熔断 / 预算事件流水，含各 Key 当月用量 |
| SEO 模块 | 后台内 | 管理 `robots.txt`、`sitemap.xml`、`llms.txt`（LLM 站点说明，markdown）、`ai.txt`（AI 代理简报，纯文本）、站点 meta（en/zh 的 title / description / OG），保存后立即生效 |
| 知识库 | 后台内 | 编辑聊天助手的 system prompt（默认含 AEOX 家族产品介绍），保存后注入每一次聊天请求 — 用户咨询 AEOX 相关内容时助手能正确回答；支持单独重置为默认 |

## 3. 技术架构

- **运行时**：Node.js ≥ 22.5（依赖内置 `node:sqlite`），ESM，TypeScript（仅可擦除语法），部署产物经 esbuild 全量打包为纯 JS（`dist/index.js`），**服务端零 npm 依赖 — 部署时无需 `npm install`**
- **框架**：Hono + @hono/node-server（均已打包进产物，仅本地开发需要）
- **存储**：SQLite（Node 内置 `node:sqlite` 的 `DatabaseSync`），单文件 `data/aeox-chat.db`，无需任何原生编译模块
- **前端**：原生 TS（esbuild 打包），无框架；视觉沿用 demo 的 "Industrial AI / Developer-First Minimalist" 风格（dark-only、Inter + Geist Mono）
- **安全**：API Key 以 AES-256-GCM 加密落盘；公开接口零 Key 材料；会话与 admin Cookie 均 HMAC 签名，admin Cookie 内嵌到期时间戳（12h 后服务端强制失效，无法通过重放绕过），HTTPS 下自动加 `Secure` 标记

### 目录结构

```
aeox-chat\
  说明文档.md          本文档
  demo\               概念演示页（保留原样，挂 /demo）
  data\               SQLite 数据文件（gitignore，勿提交）
  src\
    server\
      index.ts        Hono 入口：路由挂载、静态服务
      config.ts       环境变量读取
      db.ts           SQLite 初始化 + 建表 + seed
      crypto.ts       AES-256-GCM 加解密 + HMAC Cookie 签名
      engine\
        pool.ts       Key 池：选 Key / 冷却 / 熔断 / 预算 / 事件
        upstream.ts   OpenAI 兼容流式上游客户端
      middleware\
        quota.ts      匿名会话 + 双层限额
      routes\
        chat.ts       POST /api/chat（SSE）
        enginePublic.ts  公开引擎遥测（脱敏）
        seo.ts        robots.txt / sitemap.xml 输出
        adminAuth.ts  登录 / 登出 / 鉴权中间件
        adminKeys.ts  Key CRUD + 连接测试
        adminSeo.ts   SEO 文件与站点 meta 读写
    web\
      index.html      聊天页
      engine.html     公开引擎控制台
      admin.html      后台（Key + SEO）
      assets\         chat.ts / engine.ts / admin.ts / i18n.ts / md.ts
  build.mjs           esbuild 构建脚本
  dist\               服务端构建产物（server.js）
  public\             前端构建产物
```

### 引擎机制（核心）

```
请求 → 限额检查 → 从 Key 池选 Key ──→ 转发上游（流式）→ SSE 回传客户端
                     │
                     ├─ 选 Key 规则：status=active 且不在冷却期 且未超月预算，按 priority 升序取第一个
                     ├─ 上游 429 / 5xx：该 Key 冷却 60s + 记事件 → 换下一个 Key 重试（最多 2 次）
                     ├─ 上游 401 / 403：该 Key 永久禁用 + 记事件 → 换下一个 Key
                     └─ 重试只发生在首 token 之前：收到首个成功 chunk 后即提交该 Key，流不再中断
```

- 月预算 = 当月 `usage_daily` 累计 tokens × 单价（DeepSeek/Qwen/NVIDIA/Groq 预置单价；未知 provider 按 0 计，后台以请求/Token 数为主要参考）
- 流式请求带 `stream_options: {include_usage: true}`，末 chunk 无 usage 则按字符数估算
- **脱敏规则**：公开引擎接口只输出 provider + 池内序号、状态、聚合指标、事件；任何 Key 材料（含尾 4 位提示）不出公开接口，仅 admin 可见

## 4. 本地运行

### 环境变量

复制 `.env.example` 为 `.env` 后填写：

| 变量 | 必填 | 用途 | 生成方式 |
|---|---|---|---|
| `ADMIN_PASSWORD` | 是 | 后台登录密码 | 自定强密码 |
| `MASTER_KEY` | 是 | API Key 加密主密钥（32 字节） | `node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"` |
| `SESSION_SECRET` | 是 | Cookie HMAC 签名密钥 | 同上（另生成一个） |
| `PORT` | 否 | 监听端口，默认 3000 | 本地默认即可，Hostinger 会注入 |
| `BASE_URL` | 否 | 站点对外地址，默认 `http://localhost:3000` | 部署后改为 `https://chat.aeox.uk` |

> 注意：`MASTER_KEY` 一旦更换，数据库里已加密的 API Key 全部作废，需要重新录入。

### 命令

```powershell
npm install          # 安装依赖
npm run build        # esbuild 构建：dist/index.js + public/assets
npm start            # 启动：node dist/index.js
npm run typecheck    # tsc --noEmit 类型检查
npm run dev          # 本地开发：Node 原生跑 TS（需 Node ≥ 22.6）
```

启动后访问 `http://localhost:3000`。

### 首次启动的 seed 行为

- `keys` 表写入 5 个预置 Provider 行（无 Key，等后台填入）：

| Provider | Base URL | 默认模型 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-chat |
| Qwen | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus |
| NVIDIA NIM | `https://integrate.api.nvidia.com/v1` | meta/llama-3.3-70b-instruct |
| Groq | `https://api.groq.com/openai/v1` | llama-3.3-70b-versatile |
| OpenRouter | `https://openrouter.ai/api/v1` | （添加 Key 时填） |

- `seo_files` 表写入默认 `robots.txt`（allow all + 指向 sitemap）、`sitemap.xml`（单 URL 站点地图）、`llms.txt`（llmstxt.org 规范的 LLM 站点说明）、`ai.txt`（AI 代理纯文本简报）——已有库重启时通过 INSERT OR IGNORE 自动补齐新文件
- `site_meta` 表写入默认的 en/zh 标题与描述

## 5. 后台使用

### 登录

浏览器打开 `http://localhost:3000/admin`（线上为 `chat.aeox.uk/admin`），输入 `ADMIN_PASSWORD`。登录态为 HMAC 签名 Cookie；连续输错 5 次该 IP 封锁 10 分钟。

### 添加 Key（含自定义 Provider）

后台 "Keys" 区块 → "add key" 表单：

| 字段 | 说明 |
|---|---|
| Provider | 显示名称，如 `deepseek`、`agnes-ai.cn`、`unorouter` |
| Base URL | OpenAI 兼容端点根地址（到 `/v1` 这一级） |
| Model | 该 Key 默认使用的模型 ID |
| API Key | 上游密钥，保存后加密落盘，后台只显示尾 4 位 |
| Priority | 数字越小越优先被引擎选用 |
| Monthly budget | 月预算（美元），当月用量估算达到后该 Key 自动停用并记事件 |

填完先点 **test connection**（调上游 `/models` 验证并显示延迟），通过再保存。

> agnes-ai.cn / agnes-ai.com / unorouter.com / AMD 等未预置的服务商：拿到它们的 OpenAI 兼容文档里的 Base URL 与模型名后，用上表通用表单添加即可，无需改代码。

### SEO 模块

后台 "SEO" 区块：

- **robots.txt 编辑器**：保存后 `/robots.txt` 立即按新内容输出
- **sitemap.xml 编辑器**：保存后 `/sitemap.xml` 立即按新内容输出
- **站点 meta**：en / zh 两语言的 `title`、`description`、OG 标签；保存后聊天页 `<head>` 里的注入内容即更新
- 点击 "reset to default" 可恢复出厂内容

### 引擎日志

后台 "Engine log" 区块实时列出事件流水（route / failover / cooldown / recovered / disabled / budget），并显示各 Key 当月请求数与 token 用量。

## 6. 部署现状与重部署（Hostinger Web App）

### 线上配置（已生效）

- 预设选 **Hono**（底层即 Node.js，无碍）；运行时 Node v24.6.0
- **Entry File：`dist/index.js`** — 必须与构建输出文件名完全一致（构建产物因此从 `server.js` 改名为 `index.js`）
- Build 命令：`npm run build`；Start 命令：`npm start`
- 部署方式：上传 full 源码包（当前为 `D:\project\aeox-chat-deploy-full-v0.1.1.zip`，含 `src/`，面板内完成构建）；服务端零运行时 npm 依赖，面板装的 devDependencies 无害
- 环境变量（面板配置）：`ADMIN_PASSWORD` / `MASTER_KEY` / `SESSION_SECRET` / `BASE_URL=https://chat.aeox.uk`（`PORT` 由 Hostinger 注入，勿手工设）
- 域名 `chat.aeox.uk` 已绑定，SSL 由 Hostinger 自动签发

### 重部署流程

1. 本地 `npm run build` + 冒烟通过后，重新生成部署 zip（内容 = 项目全量，剔除 `data/`、`.env`、`node_modules/`、`生产环境变量.txt`、`*.zip`）
2. Hostinger 面板上传新 zip → Deploy
3. 部署只替换代码：`data/` 与环境变量不受影响，**已录入的 Key 与后台配置自动保留**，无需重录
4. 冒烟：完整对话一次、`/engine` 脱敏面板、`/robots.txt` `/sitemap.xml` `/llms.txt` `/ai.txt` 返回内容

### 常见问题

| 现象 | 处理 |
|---|---|
| 启动报 `node:sqlite` 不存在 / `DATABASE_SYNC` 未定义 | 运行时低于 Node 22.5：在面板把 Node 版本升到 ≥ 22.5 |
| 换了 `MASTER_KEY` 后所有 Key 报 401 | 预期行为：旧密文无法解密，需在后台重新录入 Key |
| 某家免费 Key 频繁 429 | 预期行为：引擎会自动冷却 60s 并切到下一家；预算内多配几家即可平滑 |
| 流式中途报错 | 重试只在首 token 前发生；首 token 后上游断流则该次对话返回失败态，重发即可 |
| 想清空全部数据 | 停应用 → 删除 `data/aeox-chat.db` → 重启（自动重建 + seed） |
| 中文文件（文档/HTML）被 PowerShell 读写后变乱码 | PowerShell 5.1 按 GBK 误解码 UTF-8 所致（本项目两次事故）；文件读写一律用 Node 或编辑工具，已损坏时从 git 干净提交 `git cat-file -p <commit>:<file>` 恢复 |

## 7. API 一览

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/chat` | 匿名 Cookie + 限额 | SSE 流式聊天；body `{messages:[{role,content}...]}`；限额：会话 30 条/天、IP 60 条/天 |
| GET | `/api/engine/status` | 无 | 脱敏 Key 池 + 聚合指标 |
| GET | `/api/engine/events` | 无 | 脱敏事件流水（最近 60 条） |
| GET | `/robots.txt` `/sitemap.xml` `/llms.txt` `/ai.txt` | 无 | 从 DB 输出，缺省回退默认内容（llms.txt 为 text/markdown，其余 text/plain） |
| POST | `/admin/api/login` | 密码，限速 | 登录，签发 admin Cookie |
| POST | `/admin/api/logout` | admin Cookie | 登出 |
| GET | `/admin/api/keys` | admin Cookie | Key 列表（含尾 4 位、用量、状态） |
| POST | `/admin/api/keys` | admin Cookie | 新增 Key |
| PATCH | `/admin/api/keys/:id` | admin Cookie | 启停 / 改优先级等 |
| DELETE | `/admin/api/keys/:id` | admin Cookie | 删除 Key |
| POST | `/admin/api/keys/:id/test` | admin Cookie | 连接测试（调上游 /models） |
| GET/PUT | `/admin/api/seo` | admin Cookie | SEO 文件与站点 meta 读写 |
| GET | `/admin/api/events` | admin Cookie | 完整引擎事件 + 用量统计 |

## 8. 边界与已知限制

- 深度思考/推理模式、模型选择器、用户注册、服务端对话留存均**不做**（UI 标注 coming soon）
- 重试只保证在首 token 前发生，流中途故障以失败态返回
- 未知 Provider（agnes-ai / unorouter / AMD）的端点未逐一实测，以通用表单 + 连接测试覆盖
- 本站为技术展示定位，不承诺 SLA
